const MODE = import.meta.env.MODE;
const APP_VERSION = import.meta.env.APP_VERSION;
const APP_BASE_URL = import.meta.env.APP_BASE_URL;
const APP_BASE_URL_PREFIX = import.meta.env.APP_BASE_URL_PREFIX;
const JWT_TOKEN = import.meta.env.APP_JWT_TOKEN;


export { MODE, APP_VERSION, APP_BASE_URL, APP_BASE_URL_PREFIX, JWT_TOKEN };
